See [`AGENTS.md`](AGENTS.md), and [`01-contract-rules.md`](01-contract-rules.md) for the contract
itself.

The instructions live there rather than here so that every agent reads the same file. A second copy
of the guidance would drift from the first, which is the failure this repository's own projection
rule exists to prevent.
